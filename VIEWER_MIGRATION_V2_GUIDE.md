# Viewer移行 v2 - 共有先確認 + 基本分析移植

## 今回の追加

### 誤送信防止

企業向けViewer URLを、

`report.html#name=<Spreadsheetファイル名>&r=<viewerToken>`

へ変更しました。

重要:
- `name` は人間が確認するための表示情報
- 認証は従来どおりランダムな `viewerToken`
- ファイル名が変わってもtoken自体は変えません
- 次回のデータ更新時に最新ファイル名のViewer URLを再生成します

ViewerではAPIから取得した実際のSpreadsheetファイル名を
「共有先確認」として大きく表示します。

URL内の `name` とAPIが返す実ファイル名が一致しない場合、
赤い警告を表示します。

### 管理Pages

Google Sheets作成・更新完了後に、

- 共有先確認
- 企業向けWebレポートを開く
- 企業共有用リンクをコピー

を表示します。

コピー内容:

【企業共有用】
共有先：<Spreadsheetファイル名>
有効応募分析レポート
<Viewer URL>

URLだけをコピーするより、
メール等へ貼り付けたあとも送付対象を目視確認できます。

### Spreadsheet

`00_このレポートについて` でも、

- 共有先確認
- 【企業共有用】<ファイル名> のWebレポートを開く
- 【社内用】分析管理ツールを開く

を表示します。

## Viewerへ追加移植した分析

従来MVP:
- KPI
- 年齢構成

今回追加:
- 対応状況別
- 応募月別
- 応募媒体別
- 企業ID別（表示名がある場合）
- 氏名文字種区分別
- 居住都道府県別
- 職種別
- 雇用形態別
- 求人勤務地名称別
- 勤務地都道府県別
- 勤務地・居住都道府県一致別

各表はGoogle Sheets版と同じ基本構造です。

列:
- 年齢区分各列
- 総応募数
- 応募構成比
- ターゲット応募数
- ターゲット率
- ターゲット応募構成比

最下部:
- 【総計】
- 年齢構成比

横幅が大きい表でもPages側では各表が独立して横スクロールするため、
Google Sheetsのように上下の表で列幅を共通化する制約はありません。

## 差し替え

GitHub Pages:
`viewer-migration-v2-pages.zip`

ZIP内:
- index.html
- report.html
- analysis-core.js

中央API:
`api_webapp_v1_9_viewer_share_guard.txt`

Template bound GAS:
- 今回は v5 のままで変更不要です。

## 確認

1. API v1.9へ差し替えてWeb App再デプロイ
2. ZIP内3ファイルをPagesへ配置
3. 管理Pagesから既存Sheetを1回更新
4. Viewer URLに `#name=...&r=...` が含まれることを確認
5. Viewer上部「共有先確認」が実ファイル名と一致
6. 管理Pagesの「企業共有用リンクをコピー」を確認
7. Sheet `00_このレポートについて` の共有先確認を確認
8. 基本分析11種の数値を既存Google Sheets版と比較

## 次工程

ここまで数値一致すれば、次は以下をViewerへ移植します。

- 仕事名KW
- 仕事名フルKW
- 募集背景
- 月内応募日
- 応募曜日
- 応募時間帯
- 給与4種
- 求人原稿文字数
- 画像有無・枚数
- 動画有無
- Indeed求人タグ数

その後、
- 求人備考1行目
- Indeedタグ詳細
- TOP画像詳細
- カスタム分析

へ進みます。
